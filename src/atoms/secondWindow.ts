/** Stable label for the Tauri secondary {@link WebviewWindow}. */
export const SECOND_WINDOW_LABEL = 'second-window';

/** URL query flag that marks the secondary window route. */
export const SECOND_WINDOW_QUERY_PARAM = 'secondWindow';
